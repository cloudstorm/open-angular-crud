"use strict"

angular.module("todo-list", []).controller "<%= class_name %>Ctrl", [ "$scope", "sessionSettings", ($scope, sessionSettings) ->
  $scope.resourceType = "<%= javascript_resource_name %>"
]
