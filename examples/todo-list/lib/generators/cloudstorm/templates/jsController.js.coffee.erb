"use strict"

angular.module("<%= application_name %>", []).controller "<%= class_name %>Ctrl", [ "$scope", "sessionSettings", ($scope, sessionSettings) ->
  $scope.resourceType = "<%= javascript_resource_name %>"
]
