/*
describe('ItemListItemController', function(){

  var $componentController;
  var csItemList;

  beforeEach(angular.mock.module('cloudStorm.itemList'))

  beforeEach(inject(function(_$componentController_){
    $componentController = _$componentController_;
  }))

  it('controllerExists', function(){
    expect($componentController).toBeDefined();
  })

  it('firstComponentTest', function(){
    var ctrl = $componentController('csItemList', null, {})
    expect(ctrl.testValue()).toEqual("test")
  })

})
*/
