.modal-header
  %button.close{ "ng-click" => "$ctrl.close()"}
    &times
  %h4.modal-title
    {{ $ctrl.UI.fieldName }}

%cs-item-list{ "item" => "$ctrl.item",
                "item-list" => "$ctrl.itemList",
                "many" => "$ctrl.many",
                "field" => "$ctrl.field",
                "modal-mode" => "true",
                "form-mode" => "$ctrl.formMode",
                "key" => "$ctrl.key"}
